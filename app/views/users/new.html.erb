<h1>Sign up</h1>

<% form_for(@user) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "fields", :locals => { :f => f } %>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>
